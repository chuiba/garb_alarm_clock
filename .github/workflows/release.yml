name: Release

on:
  push:
    tags:
      - 'v*'  # Triggers on version tags like v1.0.0, v0.1.7, etc.

jobs:
  # Temporarily disabled until PyPI trusted publisher is configured
  # build-and-publish:
  #   name: Build and Publish to PyPI
  #   runs-on: ubuntu-latest
  #   environment: pypi
  #   permissions:
  #     id-token: write
  #     contents: read

  #   steps:
  #   - name: Checkout code
  #     uses: actions/checkout@v4

  #   - name: Set up Python
  #     uses: actions/setup-python@v5
  #     with:
  #       python-version: '3.11'

  #   - name: Install dependencies
  #     run: |
  #       python -m pip install --upgrade pip
  #       pip install build twine

  #   - name: Build package
  #     run: python -m build

  #   - name: Publish to PyPI
  #     uses: pypa/gh-action-pypi-publish@release/v1

  build-windows-exe:
    name: Build Windows Executable
    runs-on: windows-latest
    permissions:
      contents: write

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller

    - name: Build executable
      run: python build_exe.py

    - name: Get tag name
      id: tag
      run: echo "tag=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
      shell: bash

    - name: Create Release and Upload Assets
      uses: softprops/action-gh-release@v2
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ steps.tag.outputs.tag }}
        name: ${{ steps.tag.outputs.tag }}
        draft: false
        prerelease: false
        files: ./dist/garb_alarm_clock.exe
        body: |
          ## Release ${{ steps.tag.outputs.tag }}

          ### 📥 Downloads
          - **Python Package**: `pip install garb-alarm-clock==${{ steps.tag.outputs.tag }}`
          - **Windows Executable**: Download `garb_alarm_clock_windows_x64.exe` below

          ### 🔄 Changes
          See commit history for detailed changes in this release.